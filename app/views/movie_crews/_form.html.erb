<%= form_with(model: movie_crew, local: true) do |form| %>
  <% if movie_crew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_crew.errors.count, "error") %> prohibited this movie_crew from being saved:</h2>

      <ul>
        <% movie_crew.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.text_field :movie_id %>
  </div>

  <div class="field">
    <%= form.label :cast_crew_detail_id %>
    <%= form.text_field :cast_crew_detail_id %>
  </div>

  <div class="field">
    <%= form.label :department_id %>
    <%= form.text_field :department_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
